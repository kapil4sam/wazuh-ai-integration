# Wazuh Threat Hunter — requirements
# Install: pip install -r requirements.txt

fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
python-multipart

# LangChain stack
langchain>=0.2.0
langchain-community>=0.2.0
langchain-openai>=0.1.8     # ChatOpenAI + OpenAIEmbeddings → covers both LLM and embeddings

# Vector store (FAISS — CPU only, no local model needed, just indexes vectors)
faiss-cpu>=1.8.0

# REMOVED: langchain-huggingface, sentence-transformers
# (were only needed for local all-MiniLM-L6-v2; now using OpenRouter embeddings API)

# Optional — remote log pull
paramiko>=3.4.0

# Optional — daemon mode
python-daemon>=2.3.2

# HTTP (pulled transitively, listed for clarity)
httpx>=0.27.0
